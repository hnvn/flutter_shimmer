import 'package:flutter/material.dart';

class LabelShimmer extends StatelessWidget {
  const LabelShimmer({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 36,
      width: 150,
      color: Colors.white,
    );
  }
}
